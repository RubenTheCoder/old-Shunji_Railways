This code has special code header, to make it easier to find code.
They are placed above important segments, like the header module
The cubes are made of Alt-code 219



SVG's need to be recolored using a color attribute within the SVG file.
Add this snippet behind the path attribute:

fill="your color"